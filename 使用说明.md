# Chrona 使用说明

## 启动

1. 双击 `启动项目管理系统.bat`。
2. 首次启动会自动安装运行组件，需要保持网络连接，可能等待几分钟。
3. 浏览器会自动打开 `http://localhost:3000`。
4. 黑色命令窗口需要保持打开；关闭窗口后系统会停止运行。

如果启动失败，黑色窗口会保留错误提示。详细原因同时记录在 `startup-error.log`。

## 关闭

- 直接关闭启动时出现的黑色窗口；或
- 双击 `关闭项目管理系统.bat`。

## 第一次使用

1. 点击“新建项目”，填写项目名称、描述、目标和日期。
2. 点击左下角“模型设置”，填入 API 地址、API Key 和模型名称。
3. 点击“测试连接”，成功后保存。
4. 回到项目，点击右上角“智能安排”或任务工作台中的“重新规划项目”。
5. AI 生成方案后先查看预览，确认无误再应用。

常用接口示例：

| 服务 | API 地址 | 模型名称示例 |
|---|---|---|
| OpenAI | `https://api.openai.com/v1` | 以账号当前可用模型为准 |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| Ollama 本机模型 | `http://localhost:11434/v1` | 已下载的模型名称 |

## 数据保存位置

所有项目数据和模型设置默认保存在：

`data/project-manager.db`

备份时，先关闭系统，再复制整个 `data` 文件夹。这个文件夹包含项目内容和模型密钥，请勿发送给其他人。

## 当前版本包含

- 创建、编辑和管理多个项目；
- 列表、状态看板、四象限和甘特图四种任务视图；
- 拖拽任务调整顺序、状态和优先级；
- 点击任意任务查看并修改完整内容；
- AI 根据聊天内容规划行程、时间和任务，并可确认后自动写入；
- 接入 OpenAI 兼容的 AI 模型；
- 多套现代配色、GitHub 风格暗黑主题、多种字体和四档字号；
- 页面运行期间的到期桌面通知；
- 本机 SQLite 数据持久化。

当前提醒需要页面处于运行状态；暂未包含关机后仍可触发的系统计划任务和多人协作。

## 字体说明

Merriweather 依据 SIL Open Font License 随项目提供。Anthropic Sans 仅从本机字体目录复制到本机运行环境，不上传到公开仓库；在其他设备缺少该字体文件时会自动回退到系统字体。
